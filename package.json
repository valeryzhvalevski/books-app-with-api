{
  "name": "serv1",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "json":"json-server --watch db.json --port 3001"
  },
  "devDependencies": {
    "vite": "^5.2.0"
  },
  "dependencies": {
    "json-server": "^1.0.0-alpha.23"
  }
}
